# 云游山河：当前开发约定

版本v1.4.0，H5手机研究原型。用户选择了路线A与B类景点图案纪念章。正式医院服务与具体可穿戴型号尚未接入，不宣称临床验证完成。

## 不变量

- 演示/正式/旧版记录分区；旧数据不推定为正式记录，也不自动删除。
- 正式训练只能接收可信提供方验证且结构有效的计划；默认PlanGateway无提供方并阻断正式入口。
- 不适停止优先于弹窗、输入和奖励；同一Game实例终止后不可重新start或resume。
- 普通暂停恢复必须复核；设备丢失接触、断连、过期、换源要暂停；明确降级后断开旧源。
- 正常结束保留POSTCHECK，正向反馈保存且用户离开复核页后才能finalize；安全结束和结束后不适保持阻断。
- 本地写入失败必须显式报告，保留持久保底标记和本页内存阻断；不能用已保存语音掩盖失败。
- 不复制指南表格为自动处方。演示参数明确标注为测试参数；真实阈值、量表与动作由康复团队审定。
- 只使用合成数据测试，不自行连接真实患者、医院、摄像头或可穿戴设备。

## 架构

session-safety.js唯一管理安全状态；rehab-plan.js维护计划来源/校验；hr.js只处理设备与样本质量；storage.js维护记录与保底标记；main.js连接UI；game.js执行游戏。

美术为assets/art中的静态SVG，Game-icons署名见CREDITS.md。保留已确认的青绿设计与纪念章。医学逻辑改动不要混入绘制函数。

## 验证

运行npm test、node tests/test_sitstand.mjs及语法/差异检查。浏览器使用模拟输入，验证未前检阻断、正式无计划阻断、暂停复核、症状急停、刷新持久化、记录分区和正常结束后的反馈。

前端改动必须更新sw.js版本及资源清单；只有实际完成的步骤才能写成已实现。项目无编译步骤，node server.js提供本地预览。

旧说明保留在docs/archive/pre-v1.4-CLAUDE.md，仅供追溯，不覆盖当前约定。
